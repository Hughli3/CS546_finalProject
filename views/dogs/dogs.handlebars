<section class="section">
  <div class="container">
    <div class="row">
      {{#each dogs}}
      <div class="col-md-3 mb-4">
          <div class="card">
              <img src="/public/img/dog/1.jpg" class="card-img-top" alt="...">
              <div class="card-body">
                  <h1 class="card-title">{{dogName}}</h1>
                  <p class="card-text">{{type}} {{gender}}</p>
              </div>
          </div>
      </div>    
      {{/each}}
    </div>
    {{#pagination totalPage currentPage}}
    <nav>
      <ul class="pagination">
        {{#if @previousPage}}
        <li class="page-item">
          <a class="page-link" href="?page={{@previousPage}}">
            <i class="fa fa-angle-left"></i>
            <span class="sr-only ">Previous</span>
          </a>
        </li>
        {{/if}}
        
        {{#paginationItem totalPage currentPage}}
        <li class="page-item {{@active}}"><a class="page-link" href="?page={{@page}}">{{@page}}</a></li>
        {{/paginationItem}}

        {{#if @nextPage}}
        <li class="page-item">
          <a class="page-link" href="?page={{@nextPage}}">
            <i class="fa fa-angle-right"></i>
            <span class="sr-only">Next</span>
          </a>
        </li>
        {{/if}}
      </ul>
    </nav>
    {{/pagination}}
  </div>
</section>

